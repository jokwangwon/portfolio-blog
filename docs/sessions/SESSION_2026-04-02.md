# 세션 로그 - 2026-04-02

**날짜**: 2026-04-02
**AI 에이전트**: Claude Code (Opus 4.6)
**세션 번호**: #13–#14

---

## 세션 목표
하네스 엔지니어링 도입 후 소셜 로그인(OAuth2) 오류 체계적 점검 및 수정, 블로그 에디터 버그 수정

## 완료된 작업

### 하네스 엔지니어링 준수 확인
- [x] 세션 프로토콜 수행 (CONTEXT.md, 최신 세션 로그 확인)
- [x] 하네스 인프라 6개 Layer 전수 점검 — 모두 정상 동작
- [x] `scripts/validate-docs.sh` 실행 — 124개 참조 검증, 0개 broken

### OAuth2 소셜 로그인 4건 이슈 수정
- [x] **CRITICAL**: Google `401 invalid_client` — `.env.local` + Gradle bootRun 환경변수 로딩
- [x] **CRITICAL**: refresh_token 쿠키 도메인 불일치 — `POST /auth/oauth-session` 엔드포인트 추가
- [x] **HIGH**: GitHub null email → DB 크래시 — fallback 이메일 생성
- [x] **MEDIUM**: 미인증 401 대신 403 반환 — `SC_UNAUTHORIZED` 수정 + 테스트 2건 업데이트

### 블로그 에디터 버그 수정 (세션 #14)
- [x] **한글 제목 slug 생성 실패**: `generateSlug()`가 non-ASCII 문자를 모두 제거하여 빈 slug 생성 → `generateUniqueSlug()` 추가 (timestamp fallback + 중복 시 suffix 자동 추가)
- [x] **Tiptap SSR hydration 에러**: `useEditor`에 `immediatelyRender: false` 옵션 추가
- [x] **슬래시 명령어 "/" 잔존 버그**: 명령어 클릭 시 "/"가 삭제되지 않는 문제 — 문서 위치(`slashDocPos`) ref로 저장하여 정확한 삭제 범위 계산

### 인프라 개선
- [x] `backend/.env.example` 생성 (OAuth2 환경변수 템플릿)
- [x] `build.gradle.kts` bootRun에서 `.env.local` 자동 로드
- [x] `application-dev.yml` 플레이스홀더를 `not-configured`로 변경

### 문서화
- [x] `docs/review/oauth2-issue-report.md` 작성 (4건 이슈 상세 보고서)
- [x] 세션 로그 작성 (본 문서)
- [x] `docs/sessions/README.md` 갱신
- [x] `docs/CONTEXT.md` 갱신

## 수정된 파일 (15개 + 신규 3개)

### 백엔드 (11개)
| 파일 | 변경 |
|------|------|
| `security/.../OAuth2AuthenticationSuccessHandler.java` | 쿠키 제거, refreshToken fragment 포함 |
| `security/.../CustomOAuth2UserService.java` | null email fallback |
| `security/.../config/SecurityConfig.java` | 401 반환, oauth-session permitAll |
| `module-user/.../controller/AuthController.java` | POST /auth/oauth-session 추가 |
| `module-blog/.../service/PostService.java` | `generateUniqueSlug()` 추가 (한글 slug + 중복 해결) |
| `module-blog/.../service/PostServiceTest.java` | 중복 slug 테스트 → suffix 검증으로 변경 |
| `api-server/build.gradle.kts` | bootRun .env.local 로딩 |
| `api-server/.../application-dev.yml` | 플레이스홀더 → not-configured |
| `api-server/.../AuthApiIntegrationTest.java` | 403→401 |
| `api-server/.../BlogApiIntegrationTest.java` | 403→401 |
| `security/.../OAuth2AuthenticationSuccessHandlerTest.java` | 쿠키 검증 변경 |

### 프론트엔드 (2개)
| 파일 | 변경 |
|------|------|
| `frontend/app/auth/callback/page.tsx` | refreshToken 추출 + oauth-session 호출 |
| `frontend/src/modules/blog/components/editor/RichEditor.tsx` | SSR hydration fix + 슬래시 명령어 "/" 삭제 버그 수정 |

### 인프라/문서 (5개)
| 파일 | 변경 |
|------|------|
| `backend/.env.example` | 신규 (환경변수 템플릿) |
| `docs/review/oauth2-issue-report.md` | 신규 (이슈 보고서) |
| `docs/sessions/SESSION_2026-04-02.md` | 신규/갱신 (본 세션 로그) |
| `docs/sessions/README.md` | 세션 #14 추가 |
| `docs/CONTEXT.md` | 최신 상태 반영 |

## 주요 결정
- OAuth2 환경변수는 `.env.local` 파일로 관리 (git 무시, `.env.example`은 git 추적)
- Gradle `bootRun` 태스크에서 `.env.local` 자동 로드
- OAuth2 refresh_token은 프론트엔드 callback에서 Next.js 프록시 경유로 쿠키 설정
- 한글 제목 slug는 `post-{timestamp}` 형식으로 생성, 중복 시 자동 suffix 추가
- Tiptap SSR 환경에서 `immediatelyRender: false` 필수

## 검증 결과
- `./gradlew clean check --no-build-cache`: BUILD SUCCESSFUL (전체 테스트 통과)
- `npx next build`: BUILD SUCCESSFUL
- API 테스트: 한글/영어 제목 글 작성 201 Created, 프론트엔드 프록시 경유 글 작성 성공

## 다음 세션 작업
1. `.env.local` 파일에 실제 Google/GitHub OAuth 키 입력 후 E2E 테스트
2. 요구사항 vs MVP 비교 분석 → "진짜 구현" 단계 전환
3. UI 시각적 점검 (Glassmorphism 디자인 적용 확인)

## 이슈 및 주의사항
- `ServiceHealthChecker`가 `ai-api-server:8000` health check 실패 로그를 출력함 (로컬에 AI 서버 미실행) — 정상적인 경고, 수정 불필요
- OAuth2 테스트 시 실제 Google/GitHub OAuth 키가 필요함 (`.env.example` 참조)
