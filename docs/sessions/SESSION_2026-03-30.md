# 세션 로그 - 2026-03-30

> **세션 #3**: CONTEXT.md 현행화, 아키텍처 전환 (독립 서비스 + 중앙 포털), 전체 문서 일관성 확보

**날짜**: 2026-03-30
**AI 에이전트**: Claude (Opus 4.6)

---

## 세션 목표

1. CONTEXT.md 현행화 (Phase 정보, 완료된 개발 작업, 아키텍처 전환 태스크 추가)
2. 독립 서비스 + 중앙 포털 아키텍처 설계
3. 전체 문서 일관성 확보

---

## 완료된 작업

### 1. CONTEXT.md 현행화
- [x] Phase 정보 갱신
- [x] 완료된 개발 작업 반영
- [x] 아키텍처 전환 태스크 추가

### 2. 독립 서비스 + 중앙 포털 아키텍처 설계
- [x] ADR-006 작성 (Independent Services + Service Registry + Nginx Gateway)
- [x] blog-architecture-context.md 전면 재작성 (포트폴리오 포털 시스템 아키텍처)
- [x] depth-2-module-structure.md 전면 재작성 (4개 섹션: Frontend Shell, Portal API, AI Benchmark API, 새 서비스 추가 가이드)

### 3. 기존 문서 일관성 갱신 (Phase 1: CRITICAL 문서 4개)
- [x] DEVELOPMENT_GUIDE.md: 서비스 간 통신 규칙 섹션 추가, API prefix 변경
- [x] ADR-001: Partially Superseded 처리, 서비스별 논리 DB 분리 설명 추가
- [x] database-consolidation-design.md: Phase 경계 안내, DB 소속 표기
- [x] PROJECT_CONSTITUTION.md: 제2조 3항 (서비스 독립성 규칙) 추가

### 4. 문서 일관성 갱신 (Phase 2: HIGH 문서 4개)
- [x] pixel-office-design.md: 서비스 경계(Portal 내부 모듈) 명확화, API path 변경
- [x] observability-design.md: 분산 환경 Observability 섹션 추가, 용어 갱신
- [x] sessions/README.md: 세션 #3 등록
- [x] 전 문서 용어 통일 (Main API -> Portal API, com.blog -> com.portfolio, /api/v1/ -> /api/portal/)

---

## 주요 결정

| 결정 | 근거 | ADR |
|------|------|-----|
| 독립 서비스 + Service Registry + Nginx Gateway 아키텍처 채택 | 서비스 독립 배포, 확장성, 기술 스택 자유도 확보 | ADR-006 |
| 서비스별 물리 DB 분리 (portal-db:5432, ai-bench-db:5433) | 인프라 레벨 교차 접근 차단, 독립적 스키마 관리 | ADR-006 |
| 서비스 간 DB 교차 접근 절대 금지, REST API 통신만 허용 | 서비스 독립성 보장, 결합도 최소화 | ADR-006 |
| Pixel Office는 Portal API 내부 모듈로 확정 | 별도 서비스로 분리할 만큼의 복잡도가 없음 | -- |

---

## 이슈 및 주의사항

| 이슈 | 상태 | 비고 |
|------|------|------|
| architecture-review.md는 역사적 문서로 용어 변경하지 않음 | 확인됨 | 과거 시점의 리뷰 기록 보존 |
| README.md (루트)는 아키텍처 전환 반영 필요 | 미완료 | 다음 세션에서 처리 |
| 패키지명 통일 (com.portfolio.portal vs com.portfolio.module) 확정 필요 | 미완료 | 다음 세션에서 확정 |

---

## 미완료 / 다음 세션 작업

- [x] README.md 아키텍처 다이어그램 현행화 — 세션 #3에서 완료
- [x] Portal API 리네이밍 — 세션 #4에서 완료
- [ ] module-registry 코드 생성
- [ ] Nginx API Gateway 설정 파일 작성
- [x] DB 물리 분리 (portal-db:5432, ai-bench-db:5433) 실제 적용 — 세션 #4에서 완료

---

## 관련 링크

- [프로젝트 컨텍스트](../CONTEXT.md)
- [문서 인덱스](../INDEX.md)
- [ADR-006](../decisions/ADR-006-microservice-architecture.md)
- [포털 시스템 아키텍처](../architecture/blog-architecture-context.md)
- [모듈 구조](../architecture/depth-2-module-structure.md)

---

---

## 세션 #4 (2026-03-30 후반)

> **세션 #4**: Portal 리네이밍, DB 물리 분리, 문서 일관성 검토, SDD 명세 완성

### 완료된 작업

#### Phase 1-1: 코드 리네이밍 (blog → portal)
- [x] PortfolioBlogApplication → PortfolioPortalApplication (패키지 + 클래스명)
- [x] settings.gradle.kts: portfolio-blog-backend → portfolio-portal-backend
- [x] build.gradle.kts: portfolio-blog-api.jar → portfolio-portal-api.jar
- [x] application.yml: portfolio-blog-api → portfolio-portal-api
- [x] AuthController: /api/v1/auth → /api/portal/auth
- [x] SecurityConfig: /api/v1/* → /api/portal/* (9개소)
- [x] application-dev.yml: POSTGRES_* → PORTAL_DB_* 환경변수
- [x] docker-compose.yml: portal-db + ai-bench-db 물리 분리
- [x] .env / .env.example: DB 분리 환경변수 반영
- [x] README.md: ai-api → ai-benchmark-api 경로 수정

#### 문서 일관성 검토 (3라운드)
- [x] Round 1: 용어 통일 (블로그→포털, 논리→물리 분리)
- [x] Round 2: 아키텍처 정합성 (cross-DB FK 제거, 마이그레이션 분리)
- [x] Round 3: 관제 에이전트 최종 감사 (선결조건 4건 해결)

#### SDD 명세 완성 (18개 CRITICAL 해결)

**1단계: 보안 명세 (C7,C8,C13,C14,C17)**
- [x] 엔드포인트별 권한 매트릭스 (공개/인증/ADMIN 3단계)
- [x] 비밀번호 정규식: `^(?=.*[a-zA-Z])(?=.*[0-9])[a-zA-Z0-9!@#$%^&*()_+\-=]{8,100}$`
- [x] SecurityFilterChain 상세 설정 (Java 코드 예시)
- [x] CORS 정책 (Origin, Methods, Headers, Credentials, MaxAge)
- [x] JWT 상세: HS256, Claims 6개, Rotation 5단계 동작, Access Token 메모리 저장

**2단계: API 명세 (C6,C9,C10,C11,C12,C15)**
- [x] /health, /api/summary 엔드포인트 openapi.yaml 추가
- [x] Validation 실패 시 errors 배열 형식 정의
- [x] DRAFT 접근 권한: 비인증→PUBLISHED만, 작성자→전체, ADMIN→전체
- [x] Slug 중복: 타임스탬프 suffix 자동 추가
- [x] 누락 엔드포인트 8개: 검색, 좋아요, 카테고리 CRUD, 태그 CRUD, 댓글 수정/삭제
- [x] 에러코드 전체 21개 정의 (6개 도메인별 분류)

**3단계: DB 스키마 (C1,C2,C3,C4,C5)**
- [x] refresh_tokens.token_family 정합 (ERD ↔ SQL 일치)
- [x] V6 seed 데이터: admin 계정 + 카테고리 6개 + 태그 10개 + 서비스 등록
- [x] service_registry/service_cache DDL 완전 정의
- [x] gpu_metrics: "PK 없음 (time 기반 파티셔닝)" 명시
- [x] posts.excerpt: VARCHAR(200) 타입 명시
- [x] likes 테이블 신규 추가 (user_id, post_id 복합 PK)

**4단계: 인프라 (C16,C18)**
- [x] .env.example: JWT_ISSUER, CORS, LOGGING, FLYWAY, DB_HOST 등 추가
- [x] module-benchmark 표현 통일 ("독립 서비스로 완전 분리")

### 최종 SDD 준비도: 9.5/10

### 주요 결정

| 결정 | 내용 |
|------|------|
| JWT 알고리즘 | HS256 (HMAC-SHA256) |
| Access Token 저장 | 클라이언트 메모리 (XSS 방지, localStorage 금지) |
| Access Token 만료 | 15분 (900,000ms) |
| 비밀번호 정책 | 영문+숫자 필수, 특수문자 선택, 8~100자 |
| 역할 | USER, ADMIN 2개 (Phase 1) |
| 댓글 깊이 | 최대 2단계 (댓글 → 답글) |
| Slug | 수정 불가, 중복 시 타임스탬프 suffix |
| Excerpt | VARCHAR(200), 미제공 시 content 앞 200자 자동 생성 |

---

## 다음 세션 작업 (Phase 1A: SDD 코딩)

### 권장 순서
1. **Flyway 마이그레이션 V1~V6** 실제 SQL 파일 생성
2. **Portal API Core**: JPA Entity, Repository, GlobalExceptionHandler
3. **Auth 엔드포인트**: signup, login, refresh, logout (JWT + Rotation)
4. **Blog CRUD**: posts, categories, tags, comments, likes
5. **Service Registry**: module-registry (폴링, 캐싱)
6. **AI Bench DB**: Alembic 마이그레이션 001~003
7. **AI Benchmark API**: /health, /api/summary Contract 구현
8. **Nginx Gateway**: URL 패턴별 라우팅

---

**세션 #4 종료**: 2026-03-30

---

## 세션 #5: Git 워크플로우 보완 + 멀티 에이전트 시스템 설계

**날짜**: 2026-03-30
**AI 에이전트**: Claude (Opus 4.6)

### 세션 목표
1. Git 브랜치/push 정책 점검 및 보완
2. GitHub Actions CI 파이프라인 구축
3. 멀티 에이전트 합의 시스템 설계 문서화

### 완료된 작업

- [x] Git 워크플로우 점검 — 6개 항목 중 3개 미흡 확인 (push 정책, CI/CD, 릴리스)
- [x] `develop` 브랜치 생성 (main에서 분기)
- [x] DEVELOPMENT_GUIDE.md 보완:
  - Phase별 브랜치 운영 정책 테이블 추가
  - Push 정책 (main 직접 push 금지, develop은 Phase 1만 허용)
  - 브랜치 생명주기 워크플로우 추가
  - 개발 상태 추적 섹션 (GitHub Issues, 마일스톤, 버전 태깅)
- [x] `.github/workflows/ci.yml` 생성:
  - Backend Build & Test (PostgreSQL 서비스 컨테이너)
  - Code Style Check (Checkstyle)
  - PR Gate (main 병합 전 모든 체크 통과 필수)
- [x] `.github/PULL_REQUEST_TEMPLATE.md` 생성
- [x] 멀티 에이전트 시스템 설계 문서 작성 (`multi-agent-system-design.md`):
  - 검토 에이전트 1 + 분석 에이전트 3 (구현/품질/대안) 아키텍처
  - 5-Phase 실행 프로토콜 (분배 → 독립 분석 → 교차 비교 → 합의 도출 → 보고)
  - 에이전트 간 출력 비교 매트릭스 형식 정의
  - 적용 시나리오 3종 (코드 개발, 문서 검토, 기술 의사결정)
  - 적용 기준 테이블 (단순=1에이전트, 복잡/보안=3에이전트)
- [x] ADR-007 작성 (멀티 에이전트 합의 시스템)
- [x] INDEX.md, decisions/README.md 업데이트

### 주요 결정
- Phase 1에서 develop 직접 push 허용, main은 PR 필수
- Phase 2부터 feature→develop도 PR 필수
- Semantic Versioning 채택 (v0.1.0 ~ v1.0.0)
- 3-에이전트 합의 시스템을 아키텍처/SDD/보안 결정에 필수 적용 (ADR-007)

### 생성된 파일
| 파일 | 유형 |
|------|------|
| `.github/workflows/ci.yml` | CI 파이프라인 |
| `.github/PULL_REQUEST_TEMPLATE.md` | PR 템플릿 |
| `docs/architecture/multi-agent-system-design.md` | 설계 문서 |
| `docs/decisions/ADR-007-multi-agent-consensus-system.md` | ADR |

---

**세션 #5 종료**: 2026-03-30

---

## 세션 #6: CLAUDE.md 생성 + 서비스 레이어 단위 테스트 + 인프라 구현

**날짜**: 2026-03-30
**AI 에이전트**: Claude (Opus 4.6)

### 세션 목표
1. CLAUDE.md 프로젝트 지시 파일 생성
2. 기존 서비스 레이어 전체 단위 테스트 작성 (TDD 보완)
3. Health Check + Service Contract 구현
4. Logback JSON 구조화 로깅 설정

### 완료된 작업

#### 1. CLAUDE.md 생성
- [x] 프로젝트 루트에 CLAUDE.md 생성 (SDD+TDD, 3+1 에이전트, 세션 프로토콜, Git 워크플로우)
- [x] memory/dev_preferences.md에 TDD 필수 적용 반영

#### 2. 서비스 레이어 단위 테스트 (62개 메서드)
- [x] CategoryServiceTest (9 tests): CRUD, 중복 이름, not found
- [x] TagServiceTest (7 tests): CRUD, 중복 이름, not found
- [x] PostServiceTest (18 tests): CRUD, 검색, 좋아요/취소, slug, excerpt 자동생성, 상태전환, 소유권 검증
- [x] CommentServiceTest (9 tests): CRUD, 2단계 깊이 제한, soft delete, 소유권 검증
- [x] AuthServiceTest (11 tests): 회원가입, 로그인, 토큰 갱신/rotation, 로그아웃, 중복 체크
- [x] GlobalExceptionHandlerTest (8 tests): 모든 HTTP 상태코드 및 에러 응답 형식

#### 3. Health Check + Service Contract
- [x] HealthController: GET /health (status, service, timestamp)
- [x] HealthController: GET /api/summary (service info, version, endpoint list)
- [x] HealthControllerTest (4 tests)

#### 4. Logback JSON 로깅
- [x] logback-spring.xml 생성 (dev: human-readable, prod: structured JSON)
- [x] logstash-logback-encoder 의존성 추가
- [x] MdcFilter: request_id, request_path MDC 설정 + X-Request-Id 응답 헤더
- [x] MdcFilterTest (3 tests)

### 테스트 현황
| 모듈 | 테스트 파일 | 메서드 수 |
|------|------------|----------|
| module-blog | CategoryServiceTest | 9 |
| module-blog | TagServiceTest | 7 |
| module-blog | PostServiceTest | 18 |
| module-blog | CommentServiceTest | 9 |
| module-user | AuthServiceTest | 11 |
| api-server | GlobalExceptionHandlerTest | 8 |
| api-server | HealthControllerTest | 4 |
| api-server | MdcFilterTest | 3 |
| **합계** | **8개 파일** | **69개** |

### 커밋 내역
| 커밋 | 내용 |
|------|------|
| `e167c21` | docs: Add CLAUDE.md project directive file |
| `2900c02` | test: Add unit tests for all service layers and exception handler |
| `0b03640` | feat: Add health check, service contract, and structured logging |

---

**세션 #6 종료**: 2026-03-30

---

## 세션 #7: module-registry 생성 + Controller 테스트

**날짜**: 2026-03-30
**AI 에이전트**: Claude (Opus 4.6)

### 세션 목표
1. module-registry 모듈 생성 (Service Registry CRUD)
2. Controller 레이어 단위 테스트 작성
3. 전체 테스트 90개 달성

### 완료된 작업

#### 1. module-registry 모듈 생성
- [x] build.gradle.kts + settings.gradle.kts 설정
- [x] ServiceRegistryService: 전체 조회, 개별 조회, 등록, 비활성화
- [x] ServiceStatusResponse DTO: registry + cache 합산 응답
- [x] ServiceRegistryRequest DTO: 서비스 등록 요청
- [x] ServiceRegistryController: GET/POST/DELETE (ADMIN 권한)
- [x] SecurityConfig: GET /api/portal/services/** 공개 접근 허용
- [x] ServiceRegistryServiceTest (8 tests)

#### 2. Controller 레이어 단위 테스트
- [x] CategoryControllerTest (4 tests): GET/POST/DELETE 응답 코드 검증
- [x] PostControllerTest (9 tests): GET/POST/PUT/DELETE/like/unlike 응답 검증

### 테스트 현황 (누적)
| 모듈 | 파일 | 메서드 수 |
|------|------|----------|
| module-blog | Service 4개 + Controller 2개 | 56 |
| module-user | AuthServiceTest | 11 |
| module-registry | ServiceRegistryServiceTest | 8 |
| api-server | ExceptionHandler + Health + MdcFilter | 15 |
| **합계** | **11개 파일** | **90개** |

### 커밋
| 커밋 | 내용 |
|------|------|
| `986229b` | feat: Add module-registry and controller unit tests |

---

**세션 #7 종료**: 2026-03-30

### 다음 세션 작업 (Phase 1A 잔여)
1. Nginx API Gateway 설정
2. Sentry 연동
3. ServiceHealthChecker (@Scheduled 헬스체크 폴링)
4. Integration Test (Testcontainers)

---

## 세션 #8: ServiceHealthChecker + Nginx Gateway + Sentry 연동

**날짜**: 2026-03-30
**AI 에이전트**: Claude (Opus 4.6)

### 세션 목표
1. ServiceHealthChecker 구현 (@Scheduled 폴링)
2. Nginx API Gateway 설정
3. Sentry 에러 트래킹 연동

### 완료된 작업

#### 1. ServiceHealthChecker (@Scheduled 헬스체크 폴링)
- [x] ServiceHealthChecker: @Scheduled(fixedDelayString) 기반 헬스체크 폴링
- [x] RestTemplate으로 등록 서비스 /health 엔드포인트 호출 → ServiceCache 갱신
- [x] RegistryConfig: RestTemplate Bean (5초 connect/read timeout)
- [x] ServiceHealthCheckerTest (5 tests): 성공→UP, 실패→DOWN, 기존 캐시 업데이트, 서비스 없음, 연속 실패

#### 2. Nginx API Gateway 설정
- [x] nginx/nginx.conf 생성: upstream 2개 (portal-api, ai-api)
- [x] URL 패턴 라우팅: /api/portal/ → Portal API, /api/ai/ → AI API, / → Frontend
- [x] Proxy 헤더 설정 (Host, X-Real-IP, X-Forwarded-For, X-Forwarded-Proto)
- [x] WebSocket 지원 (Frontend HMR용 Upgrade/Connection 헤더)
- [x] docker-compose.yml에 nginx 서비스 추가 (gateway 프로필)

#### 3. Sentry 에러 트래킹 연동
- [x] sentry-spring-boot-starter-jakarta:6.34.0 + sentry-logback:6.34.0 의존성 추가
- [x] application.yml: Sentry DSN, traces-sample-rate 0.1, send-default-pii false
- [x] logback-spring.xml (prod): SentryAppender (minimumEventLevel: WARN)

### 테스트 현황 (누적)
| 모듈 | 파일 | 메서드 수 |
|------|------|----------|
| module-blog | Service 4개 + Controller 2개 | 56 |
| module-user | AuthServiceTest | 11 |
| module-registry | ServiceRegistryServiceTest + ServiceHealthCheckerTest | 13 |
| api-server | ExceptionHandler + Health + MdcFilter | 15 |
| **합계** | **12개 파일** | **95개** |

### 커밋
| 커밋 | 내용 |
|------|------|
| `2695c0e` | feat: Add health checker, Nginx gateway, and Sentry integration |

---

**세션 #8 종료**: 2026-03-30

### 다음 세션 작업 (Phase 1A 잔여)
1. @EnableScheduling 설정 (api-server)
2. Integration Test (Testcontainers)
3. module-benchmark 제거 (ai-benchmark-api 독립 서비스 분리)
4. Nginx Gateway 실제 테스트 (docker-compose 기동)

---

## 세션 #9: 통합 테스트 + module-benchmark 제거 + @EnableScheduling

**날짜**: 2026-03-31
**AI 에이전트**: Claude (Opus 4.6)

### 세션 목표
1. @EnableScheduling + module-registry 스캔 설정
2. module-benchmark 빈 모듈 제거
3. Testcontainers PostgreSQL 기반 통합 테스트 작성

### 완료된 작업

#### 1. Application 설정 보완
- [x] @EnableScheduling 추가 → ServiceHealthChecker @Scheduled 활성화
- [x] scanBasePackages에 `com.portfolio.module.registry` 추가
- [x] @EnableJpaRepositories에 `com.portfolio.domain.service.repository` 추가
- [x] @EntityScan에 `com.portfolio.domain.service` 추가

#### 2. module-benchmark 제거
- [x] backend/module-benchmark/ 디렉토리 삭제 (빈 스켈레톤)
- [x] settings.gradle.kts에서 module-benchmark 제거
- [x] api-server/build.gradle.kts에서 module-benchmark 의존성 제거

#### 3. 빌드 개선
- [x] build.gradle.kts: `-parameters` 컴파일러 플래그 추가 (Spring @PathVariable 파라미터 해석)
- [x] api-server/build.gradle.kts: JUnit Platform + 테스트 로깅 설정

#### 4. 버그 수정
- [x] GlobalExceptionHandler: `AccessDeniedException` 핸들러 추가 (403 반환)
  - 기존에는 Spring Security의 AccessDeniedException이 generic Exception handler에 잡혀 500 반환

#### 5. 통합 테스트 (28개 메서드)
- [x] IntegrationTestBase: Singleton PostgreSQL Testcontainer, 공통 DB cleanup
- [x] application-test.yml: Flyway 비활성화, JPA create-drop, JWT 테스트 키
- [x] AuthApiIntegrationTest (11 tests):
  - 회원가입 성공/중복/유효성, 로그인 성공/실패, 토큰 갱신 성공/실패
  - 로그아웃 성공, 인증 정보 조회, 로그아웃 후 토큰 무효화
- [x] BlogApiIntegrationTest (17 tests):
  - 카테고리 CRUD (공개 조회, ADMIN 생성/수정/삭제, 일반 사용자 403, 404)
  - 게시글 CRUD (생성/수정/삭제, 미인증 차단, 타인 수정 403)
  - viewCount 증가, 좋아요/좋아요 취소 + likeCount, 검색, 카테고리별 조회

### 해결한 기술 이슈
1. **Testcontainer lifecycle**: `@Container` → singleton static 블록으로 변경 (테스트 클래스 간 컨테이너 공유)
2. **FK 제약 위반**: 공통 `cleanDatabase()` 메서드로 삭제 순서 보장 (likes→comments→posts→categories→tags→refresh_tokens→users)
3. **JWT 토큰 중복**: 같은 초에 생성 시 동일 토큰 → `Thread.sleep(1100)` + `refreshTokenRepository.deleteAll()` 으로 해결
4. **`-parameters` 컴파일러 플래그**: @PathVariable 파라미터 이름 해석 실패 → Gradle에 `-parameters` 추가

### 테스트 현황 (누적)
| 모듈 | 파일 | 메서드 수 |
|------|------|----------|
| module-blog | Service 4개 + Controller 2개 | 56 |
| module-user | AuthServiceTest | 11 |
| module-registry | ServiceRegistryServiceTest + ServiceHealthCheckerTest | 13 |
| api-server (단위) | ExceptionHandler + Health + MdcFilter | 15 |
| api-server (통합) | AuthApiIntegrationTest + BlogApiIntegrationTest | 28 |
| **합계** | **14개 파일** | **123개** |

### 커밋
| 커밋 | 내용 |
|------|------|
| `f46cb96` | feat: Add integration tests with Testcontainers, remove module-benchmark |

---

**세션 #9 종료**: 2026-03-31

---

## 세션 #10 (2026-03-31)

**목표**: Phase 1A 잔여 작업 완료

### 완료된 작업

1. **JwtTokenProvider jti(UUID) claim 추가**
   - `generateToken()`에 `.id(UUID.randomUUID().toString())` 추가
   - 동일 시간대 토큰 중복 문제 근본 해결
   - 테스트에서 `Thread.sleep()` 및 `refreshTokenRepository.deleteAll()` 워크어라운드 제거

2. **Flyway 마이그레이션 통합 정리**
   - V1~V5를 단일 `V1__init_portal_schema.sql`로 통합
   - TimescaleDB 의존성 제거 (ai_models, benchmark_results, gpu_metrics 테이블 제거)
   - Portal DB 전용 스키마: users, refresh_tokens, oauth_accounts, categories, tags, posts, post_tags, comments, likes, service_registry, service_cache (11개 테이블)
   - docker-compose에서 initdb.d 볼륨 마운트 제거 (Flyway가 처리)

3. **API 서버 Dockerfile 생성**
   - 멀티 스테이지 빌드 (eclipse-temurin:17-jdk-jammy → 17-jre-jammy)
   - 비root 사용자 실행 (appuser)
   - 의존성 캐시 레이어 최적화 (Gradle wrapper → build.gradle.kts → source)
   - HEALTHCHECK 설정 (/health 엔드포인트)
   - .dockerignore 추가

4. **Nginx Gateway + 전체 docker-compose 기동 테스트**
   - `version` 속성 제거, `name: portfolio` 추가 (한글 디렉토리 호환)
   - Nginx upstream에 `resolver` + 동적 resolve 적용 (ai-api, frontend 미기동 시에도 시작 가능)
   - application.yml에 datasource/jwt/cors 환경변수 바인딩 추가
   - 전체 스택 기동 검증: portal-db → api-server (Flyway 성공) → nginx gateway
   - Gateway를 통한 /health, /api/summary, 회원가입 API 정상 동작 확인

5. **application.yml 환경변수 바인딩 보강**
   - datasource (SPRING_DATASOURCE_URL/USERNAME/PASSWORD)
   - jwt (JWT_SECRET/ACCESS_EXPIRATION/REFRESH_EXPIRATION)
   - cors (CORS_ALLOWED_ORIGINS)

### 이슈 및 해결

1. **Flyway V1 중복**: 기존 `V1__init_schema.sql`과 새 `V1__init_portal_schema.sql`이 공존 → 기존 파일 삭제
2. **Docker Alpine arm64 미지원**: `eclipse-temurin:17-jre-alpine`이 Apple Silicon에서 사용 불가 → `jammy` 기반으로 변경
3. **Nginx upstream 해석 실패**: `ai-api-server:8000` 호스트 미존재 → `set $variable` + `resolver` 패턴으로 동적 해석
4. **포트 충돌**: 로컬 PostgreSQL이 5432 사용 중 → `PORTAL_DB_PORT=5434`로 우회

### 테스트 현황 (누적)
- 전체 123개 테스트 유지 (14개 파일, 전부 통과)
- Thread.sleep 워크어라운드 제거로 통합 테스트 속도 개선

---

**세션 #10 종료**: 2026-03-31

**Phase 1A 완료!** 다음은 Phase 1B:
1. Next.js Shell App 프로젝트 생성
2. AI Benchmark API (FastAPI) 독립 프로젝트 생성
3. OAuth2 소셜 로그인 (Google, GitHub)
